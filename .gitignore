# Go
*.test
*.out

# Wails build artifacts (build/ itself is tracked: appicon.png, darwin/Info*.plist)
build/bin/

# Frontend (Vite/TS)
frontend/node_modules/
frontend/dist/
frontend/wailsjs/

# Test fixtures that must never be committed (real keychain/API captures)
**/testdata/raw/

# Local tooling state, not project source
.agentkit/

# Working notes: plans, phase files, and agent reports stay on the authoring
# machine. They record intermediate reasoning, not the shipped contract, and
# docs/ is the durable surface.
plans/

# OS
.DS_Store

# Editor
.vscode/
.idea/
